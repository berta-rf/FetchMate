from unittest import TestCase, main
from unittest.mock import patch
from src.utils import get_matched_breeds, download_dog_image, requests


# need testing for download_dog_image??
    # check if download dog image still creates a new dog file (it only does that if that dog hasn't
    # previously been added


class TestDownloadDogImage(TestCase):
    pass